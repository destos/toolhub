{% extends '_layouts/base.jinja' %}

{% block content %}
<div class="col-md-12">
    <h1>{{_('Account Settings')}}</h1>
</div>
<div class="col-md-6">
    {{crispy(form)}}
</div>
<div class="col-md-6">
    <h3>{{_('Account Actions')}}</h3>
    <a href="{{ url('account_delete') }}">{{ _("Delete account") }}</a>
    <br>
    <a href="{{ url('account_password') }}">{{ _("Update password") }}</a>
</div>
{% endblock %}
